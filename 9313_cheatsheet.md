# COMP9313 算法零基础详解（上册：地基 + MapReduce 思维 + 数据流算法）

# 第 0 章 地基：五个你必须先舒服的概念（约 40 分钟）

后面所有算法只用到这五样工具。逐个过一遍，之后遇到就不慌了。

## 0.1 哈希函数（hash function）——"打乱器"

**它是什么**：一个函数 $h(x)$，输入任何东西（单词、数字、字符串），输出一个**看起来随机、但每次都一样**的数字。"看起来随机"= 输出均匀散布；"每次都一样"= 同一个输入永远得到同一个输出（这点和随机数不同！）。

**类比**：往一排 7 个信箱里投信。哈希函数就是"按名字算出投第几个信箱"的规则——同一个人的信永远进同一个箱子，但不同人被"打散"到各个箱子。

**本课常见形态**：$h(x) = (a \cdot x + b) \bmod N$。比如 $h(x) = 3x + 2 \bmod 7$：输入 0 得 2，输入 1 得 5，输入 4 得 $14 \bmod 7 = 0$。

**冲突（collision）**：两个不同输入撞出同一个输出（信箱数量有限，人多必撞）。**后面一半的算法都在跟冲突斗智斗勇**：Bloom Filter 的假阳性、Count-Min 的高估、LSH 的"撞桶"——全是冲突在起作用。

## 0.2 取模（mod）——"绕圈计数"

$a \bmod N$ = a 除以 N 的**余数**。想象一个 N 格的钟面：从 0 出发走 a 步，停在哪格就是答案。
- $60 \bmod 7$：$60 = 8 \times 7 + 4$，答案 **4**。
- **负数的坑（考试必踩点）**：$-1 \bmod 7$ 不是 −1！在钟面上从 0 **倒退一步**，落在 **6**。规则：==结果为负就**加 N** 直到非负==。$2\times0 - 1 = -1 \to -1 + 7 = 6$。

## 0.3 一点点概率（只需要三条）

1. **独立事件同时发生 = 概率相乘**。抛两次硬币都正面：$\frac12 \times \frac12 = \frac14$。"签名的 r 行**全部**相等" = $s \times s \times \cdots = s^r$。
2. **"至少发生一次" = 1 − "一次都不发生"**。这是本课最重要的概率技巧，LSH 公式 $1-(1-s^r)^b$ 整个就是这条：先算"每个 band 都不全等"$(1-s^r)^b$，再用 1 减。正着算"至少一次"要分好多情况，反着算只有一种情况——记住"**至少→取反**"。
3. **均匀随机 = 人人平等**。从 s 个里"均匀随机挑一个"，每个被挑中概率 $1/s$。

## 0.4 对数（log）——"折半次数"

$\log_2 N$ = "N 不断除以 2，除几次到 1"。$\log_2 8 = 3$，$\log_2 10亿 \approx 30$。
**为什么算法爱它**：10 亿的数据，答案跟 30 相关而不是跟 10 亿相关，就是巨大的胜利。DGIM 的空间 $O(\log^2 N)$：窗口 10 亿位 → 只需约 $30 \times 30 = 900$ 量级的存储。看到 $O(\cdot)$ 就读作"大约正比于"。$O(1)$ = 跟数据量无关（哈希查找）；$O(N)$ = 扫一遍（list 查找）——这就是"broadcast 用 set 不用 list"的全部理由。

## 0.5 矩阵乘向量——"加权收钱"

PageRank 唯一用到的线性代数。$M \cdot r$ 的第 j 行 = "把 r 的每个分量按 M 第 j 行的权重加权求和"。
**不用会算一般矩阵**，只要会读这句话：**新的 $r_j$ = 每个链向 j 的页面 i，交出自己分数的 $\frac{1}{d_i}$**。矩阵形式只是把"所有页面同时收钱"写紧凑了。

---

# 第 1 章 MapReduce 的"算法感"（Q2 的地基，约 2 小时）

你可能会写 word count，但 Q2 考的是**设计**。设计的全部秘密在于理解一件事：

## 1.1 核心心法：shuffle 是一台免费的分拣排序机

MapReduce 框架在 map 和 reduce 之间干了一件大事（shuffle & sort）：**把所有相同 key 的数据聚到同一个 reducer，并且按 key 排好序送达**。
所以 MapReduce 算法设计的唯一自由度是：**你把什么设计成 key**。

- ==想让什么数据聚在一起== → 把它做成 key 的一部分；
- 想让数据==按什么顺序到达== → 把它做成 key 的一部分（框架只排 key 不排 value！）；
- 这两条合起来就是四大设计模式的根。

## 1.2 二次排序（value-to-key conversion）——为什么非要三件套

**问题**：为每个位置输出访问过的用户列表，用户按 ID 升序。

**笨办法**：map 发 `(loc, user)`，reducer 把 user 收进内存列表再 `sort()`。
**为什么不行**：==某热门位置有 1 亿用户 → reducer 内存爆炸==。**"在 ==reducer 内存里排序==" 是 Q2 的头号==扣分答案==**。

**聪明的点子**：框架不是免费按 key 排序吗？**把 user 塞进 key**：发 `("loc,user", "")`。这样 reducer 收到的数据天然按 (loc, user) 有序——排序白嫖了。

**但塞进 key 引发一个新 bug（这就是 partitioner 存在的意义）**：默认 partitioner 按**整个 key** 的哈希分发。`"loc1,userA"` 和 `"loc1,userB"` ==哈希不同== → **同一个位置的数据被劈到两个 reducer** → 每个 reducer 只见到半份数据，结果错。
**修复**：==自定义 partitioner **只按第 1 字段（loc）哈希**==。于是三件套缺一不可：

1. 组合键 `(loc, user)` —— 白嫖排序
2. comparator `-k1,1 -k2,2` —— 定义排序顺序（数值加 `n`，降序加 `r`）
3. partitioner `-k1,1` —— 保证同组数据不被劈开

reducer 侧配套写法：框架逐 key 调用 reduce，同一个 loc 会来很多次 → 用 `current_loc` 变量检测"换组了没"，换组就输出上一组；**循环结束后补发最后一组**（reduce_final）——漏这一行是第二号扣分点。

**在你自己的 Project 1 里**：`token = "%08d" % (99999999 - int(date))` 是二次排序的取反技巧——框架只会升序？把数值用 99999999 减一下，升序就变成了日期降序。

## 1.3 Order inversion——让"总数"插队先到

**问题**：算相对频率 $f(B|A) = \frac{count(A,B)}{count(A,*)}$，除法需要**先**知道分母（A 的总次数），但明细 (A,B) 和总数是同批数据算出来的，谁先谁后？

**聪明的点子**：给"总数"造一个**排序上抢先的特殊 key**。比如==给每个 A 额外发 `(A, "*")` 的计数==，且==保证 `"*"` 的 ASCII 排在所有正常单词前== → reducer 处理 A 组时==**第一条收到的就是总数**==，存下来，后面每条明细直接除。"把计算顺序问题变成排序问题"。
三要素：特殊 key + 控制排序（特殊符号必须排最前——`~` 排在字母后所以不行，quiz 考过）+ 控制分区（A 的所有 key 同 reducer）。
**你 Project 1 里的活例子**：`"0;..."`（overall）排在 `"B;..."`（daily）前面，reducer 先算出 overall 平均，再逐日计算——一模一样的思想。

## 1.4 Combiner / in-mapper combining——省流量的两种姿势

**Combiner**：在 mapper 本地先做小聚合（比如本地词频求和）再上网络。三条铁律：①框架**可能压根不执行它**（0/1/多次），==正确性绝不能依赖==；②==输入输出类型必须 = mapper== 输出类型（历届最大扣分点：combiner 改了类型）；③只有满足交换律+结合律的操作能直接拿 reducer 当 combiner——==**求平均不行**==：$avg(avg(1,2), avg(3)) = 2.25 \ne 2$。正解是传 `(sum, count)` 两元组，最后才除。
**In-mapper combining**：干脆在 mapper 内存里建个字典累加，处理完全部输入后在 `mapper_final/cleanup` 一次性输出。更快（数据没生成就聚合了）、**保证执行**；代价是 ==key 太多会爆内存==。考试答哪个都行，但要能说出区别。
**考场自检**：把 combiner 从你的设计里删掉，答案还对吗？不对就说明你把正确性建立在 combiner 上了——重新设计。

## 1.5 Pairs vs Stripes（30 秒版）

统计词共现：pairs 发 `((w,u),1)`——简单但 key 空间 $N^2$，==中间数据爆炸==；stripes 发 `(w, {u:1, v:2})`——key 空间 $N$、combiner 收益大，但==单条 stripe 可能撑爆内存、==reducer 要合并字典。两个你在 lab3 都写过，考前把 lab3 代码翻出来看 10 分钟。

## 🔴练习 1（做完再往下）

设计 MapReduce：输入每行 `(学生, 科目, 分数)`，输出每个科目的**最高分及并列的所有学生**，学生按名字升序。
**解**：mapper 发组合键 `("科目,分数", 学生)`？——不对！仔细想：我们要按分数**降序**找最高、按学生升序列名单，两层排序 → key = `"科目,分数,学生"`（三字段）。JOBCONF：separator `,`；partitioner `-k1,1`（只按科目）；comparator `-k1,1 -k2,2nr -k3,3`（科目升、分数数值降、学生升）。reducer：每个科目第一条的分数即最高分，继续收集分数相同的学生，分数一变小就忽略其余；换科目时输出上一组，结尾补最后一组。combiner：可做"本地最高分及并列名单"（max 满足交换结合律）。


## 🌃练习 2-1（热身：分组平均 + 排序）

输入每行 `(传感器ID, 日期yyyy-mm-dd, 温度)`。输出每个传感器**每月**的平均温度，同一传感器的月份**升序**排列。要求可扩展。

**参考答案**：
- Mapper：解析行，取月份 = 日期前 7 位（yyyy-mm）；`Emit(("传感器,月份"), (温度, 1))`——注意平均要传 (sum, count)。
- Combiner：同 key 对位相加 (sum, count)（合法：加法交换+结合）。
- Reducer：同 key 累加后相除得平均。**月份排序怎么办？** 月份已在 key 里，comparator `-k1,1 -k2,2` 即可（yyyy-mm 字典序 = 时间序，这是把日期写成 yyyy-mm 的好处）；partitioner `-k1,1` 只按传感器。
- 陷阱回顾：平均值不能直接 combiner（规律 9）；yyyy-mm 格式天然可字典序排序。

## 🌃练习 2-2（最值 + 取"第一条"：每用户最早登录）

输入每行 `(用户ID, 时间戳, IP)`。输出每个用户**最早一次**登录的时间戳和 IP。

**参考答案**：
- Mapper：`Emit(("用户,时间戳"), IP)`（value-to-key：时间戳进 key）。
- JOBCONF：partitioner `-k1,1`；comparator `-k1,1 -k2,2n`（时间戳数值**升序**——求最早，不加 r）。
- Reducer：每个用户组的**第一条**就是答案，输出后跳过该用户其余记录（用 current_user 检测换组）。
- Combiner：可做"本地最早"（min 合法）。
- 陷阱：求最早/最小 → 升序 → **不要**惯性写 `nr`。


---

# 第 2 章 蓄水池抽样——20 分钟就能会的第一个流算法

## 2.1 故事

你运营一个网站，用户点击像流水一样涌来，一天几十亿条。你想随时保有"1000 条**公平的**随机样本"用来分析。难点：流没有尽头，你永远不知道"总数"是多少——没有总数，怎么做到"每条被抽中的概率相等"？

## 2.2 笨办法为什么不行

"全存下来最后再抽"——存不下，且"最后"永远不来。"每 1000 条抽 1 条"——样本随流无限增长，而且早来的和晚来的机会也不等。

## 2.3 聪明的点子

**让新来的元素"按资历买门票"，让在池的元素"随机让位"**。第 n 个到达的元素，门票价格是 $s/n$——越晚来门票越难中（因为池子已经装着久经考验的样本）；但一旦中票，被顶替的旧元素是**均匀随机**选的，谁也没有特权。这一严一松恰好抵消，让所有人概率始终保持 $s/n$。

## 2.4 ‼️操作步骤

1. 前 $s$ 个元素：直接进池（此时每人概率 $\frac{s}{s} = 1$ ✓）。
2. 第 $n$ 个元素（$n > s$）到达：==以概率 $\frac{s}{n}$ 接纳==。
3. 若接纳：从池中 $s$ 个元素里**均匀随机**（各 $\frac1s$）踢一个，新元素顶替。若不接纳：丢弃，池不变。

## 2.5 手把手例子（s = 3，流 A B C D E F）

| 到达    | n   | 接纳概率    | 假设的结果    | 池       |
| ----- | --- | ------- | -------- | ------- |
| A,B,C | 1–3 | 直接进     | —        | {A,B,C} |
| D     | 4   | 3/4     | 中票，随机踢 B | {A,D,C} |
| E     | 5   | 3/5     | 没中       | {A,D,C} |
| F     | 6   | 3/6=1/2 | 中票，随机踢 A | {F,D,C} |

此刻（n=6）每个元素在池概率都是 $\frac36 = \frac12$。

**验证一个**（考试加分理解）：A 在 n=6 时还在池的概率 = "n=5 时在池" × "F 来了没把它顶掉" = $\frac35 \times (1 - \frac12 \times \frac13) = \frac35 \times \frac56 = \frac12$ ✓。其中 $\frac12 \times \frac13$ = "F 中票**且**恰好踢中 A"。这个两步相乘的小验证就是归纳证明的全部内容。

## 2.6 常见误区

- ✗ 踢"最老的"或"最新的"——必须**均匀随机**踢，否则概率就不再人人相等；
- ✗ 概率用总长算——quiz 原题：=="1000 个元素的流、样本 10，**第 100 个元素到达时**它进池概率？=="答 ==$\frac{10}{100}$==（用到达时刻的 n），不是 $\frac{10}{1000}$；
- ✗ 忘了前 s 个是无条件进池的。

## 🔴练习 2

s=2，流 X Y Z。(a) Z 的接纳概率？(b) 处理完 Z 后 X 仍在池的概率？(c) 验证三者概率相等。
**解**：(a) $\frac23$。(b) $1 \times (1 - \frac23 \cdot \frac12) = \frac23$。(c) X、Y 对称都是 $\frac23$，Z 是 $\frac23$，三者相等且 $= s/n = 2/3$ ✓。

a) s/n = 2/3
b) X还在的概率 = 不接纳Z + 踢走Y = (1-2/3) + 2/3 * 1/2 = 1/3 + 1/3 = 2/3
c) 

**==TODO: 多理解....==**

---

# 第 3 章 Bloom Filter——一排灯泡当门卫

## 3.1 故事

邮件系统有 10 亿个可信地址。来一封邮件要==立刻判断"发件人在不在名单里"==。存完整名单要几十 GB；==哈希表也太大==。你愿意==接受"偶尔把陌生人误认为熟人"（多收几封垃圾邮件），但**绝不能把熟人当陌生人拒掉**==（漏掉重要邮件）。

## 3.2 笨办法为什么不行

存全名单：内存爆。只存名单哈希值：还是==每人至少几字节==。我们想要的是**每人不到一个字节**的极限压缩——代价是放弃"百分之百准确"。

## 3.3 聪明的点子

**不存名单，只存"痕迹"**。准备一排 n 个灯泡（初始全灭）。每个可信地址按 k 个哈希函数算出 k 个位置，把这些灯**点亮**。查询时算同样 k 个位置：
- **有任何一个灯是灭的** → 这人**绝对**不在名单里（在名单里的人，他的灯当初必然全被点亮过，灯不会自己灭）；
- ==k 个灯全亮 → "**应该**在名单里"==——但也可能是别人点的灯恰好凑齐了（**假阳性**）。

一句话总结两条性质：**无假阴性**（灭灯 = 铁证不在）、**有假阳性**（全亮 ≠ 铁证在）。

## 3.4 操作步骤

- 插入 s：对 $i = 1..k$，置 $B[h_i(s)] = 1$（重复置 1 无所谓，不计次数）。
- 查询 x：对 $i = 1..k$ 检查 $B[h_i(x)]$。==**全 1 判"在"；任一为 0 判"不在"**==。

## ‼️3.5 手把手例子（考试原题尺寸）

==$n = 7$ 位（下标 0–6），$h_1 = \sum(\text{字母序号}) \bmod 7$（a=0,b=1,…,z=25），$h_2 = \text{长度} \bmod 7$==。
先抄好字母表：a0 b1 c2 d3 e4 f5 g6 h7 i8 j9 k10 l11 m12 n13 o14 p15 q16 r17 s18 t19 u20 v21 w22 x23 y24 z25。

插入 S = {hi, big, data}：
- hi：$h_1 = (7+8) \bmod 7 = 15 \bmod 7 = 1$；$h_2 = 2$ → 亮 1、2
- big：$h_1 = (1+8+6) \bmod 7 = 15 \bmod 7 = 1$；$h_2 = 3$ → 亮 1（已亮）、3
- data：$h_1 = (3+0+19+0) \bmod 7 = 22 \bmod 7 = 1$；$h_2 = 4$ → 亮 1、4

位数组：$[0,1,1,1,1,0,0]$（1、2、3、4 亮）。

查询 spark：$h_1 = (18+15+0+17+10) \bmod 7 = 60 \bmod 7 = 4$（亮）；$h_2 = 5$（**灭**）→ **不在**。一票否决，答案板上钉钉。

## 3.6 假阳性率公式（会代入即可，推导只需听懂一遍）

$$P(\text{假阳性}) = \left(1 - e^{-\frac{km}{n}}\right)^k$$

**变量声明（先写这行再代数！）**：$k$ = 哈希函数个数；$m$ = 已插入元素个数；$n$ = 位数组长度。上例：$k=2, m=3, n=7 \Rightarrow (1-e^{-6/7})^2 \approx 0.33$。

推导听一遍（不用背）：插入 ==m 个元素各哈希 k 次== = ==向 n 个靶子扔 $km$ 支飞镖 → 某个特定位置**始终没被击中**的概率== $\approx e^{-km/n}$ → 某位置为 1 的概率 $\approx 1 - e^{-km/n}$ → 陌生元素要蒙混过关需 k 个位置**全**为 1 → 相乘得 $k$ 次方。（用了 0.3 节"独立相乘"。）

**变量名大坑**：题面常说"a Bloom filter of size m = 7 bits"——题面的 m 是**位数**，代入公式时它是 $n$！所以答题永远先写"k=…, m=…, n=…"。

## 3.7 为什么不能删除 + Counting Bloom Filter

==删除 = 把某人的 k 个灯关掉==？不行——**灯是共享的**（上例三个词共享位 1），你关掉会误伤别人，制造假阴性，毁掉最宝贵的性质。
**==Counting BF==**：灯泡换成**计数器**，==插入 +1、删除 −1、查询看是否全 >0==。代价：每位变成小计数器，==内存翻几倍==。

==满分答案三句话==：
①不能，因为一个位可能被多个元素共享，把某元素的位清零会误伤其他元素；
②这会制造**假阴性**，破坏 Bloom Filter 最重要的"无假阴性"保证；
③解决方案是 **Counting Bloom Filter**——位换成计数器，插入 +1、删除 −1，代价是内存增大数倍。

## 🔴练习 3

用 3.5 的过滤器查 "mapreduce"（m12 a0 p15 r17 e4 d3 u20 c2 e4）。
**解**：$h_1 = 77 \bmod 7 = 0$（位 0 = **0**，灭）→ 不用再看 $h_2$，**不在**。（顺带练了"一票否决可以提前结束"。）

Solution:
h1 = (12+0+15+17+4+3+20+2+4) mod 7 = 77 mod 7 = 0 , 说明灯灭，所以直接不在，一票否决
h2 = 9 mod 7


---

# 第 4 章 DGIM——给"1"记账的智慧

## 4.1 故事

监控系统每秒发一个比特：1 = 出错，0 = 正常。你要随时回答"**最近 10 亿次里错了多少次**"。精确回答必须存整整 10 亿位。DGIM 说：给我大约 $30 \times 30$ 量级的空间，我给你一个误差不超过 50% 的答案。

## 4.2 聪明的点子：越近记得越细

记账哲学：今天的开销一笔笔记，上个月的只记总数。DGIM 把窗口里的 1 装进**桶**：新的 1 用小桶（1 个 1 一桶），旧的 1 用大桶（8 个、16 个一桶）。桶的大小必须是 2 的幂，且**同尺寸桶至多 2 个**——这一条保证桶总数只有 $O(\log N)$ 个。

每个桶记两样：$(大小 i, 时间戳 t)$，**t = 桶里最新那个 1 的时间**。

## 4.3 操作步骤（每来一个比特，固定三步）

1. **先查过期**（无论来的是 0 还是 1！）：==最旧的桶时间戳 $\le$ 当前时间 $- N$ ==→ 整桶删除。
2. 来 **0**：结束，什么都不做。
3. ~~来 **1**：新建桶 $(1, t)$。然后从小到大检查："尺寸 1 的桶有 3 个了吗？"有 → 合并**最旧的两个**成尺寸 2（时间戳取**两者中较新的**）。合并后"尺寸 2 有 3 个了吗？"……像二进制加法进位一样级联向上。~~
4. 来的是 **1**：新建桶 (1, 当前时间)。然后从小到大检查：==某尺寸凑满 **3 个==** → 把**最旧的两个**合并（大小相加、**时间戳取两者中较新的**），合并可能像进位一样级联向上。
	1. 关键在于**停止条件是"检查到某一级不足 3 个"**
	2. 若当前尺寸的桶有 **3 个** → 把**最旧的两个**合并（大小相加、时间戳取两者中**较新**的），然后**继续检查下一级尺寸**（因为刚生成了一个更大的桶，可能又凑满 3 个）；
	3. 若当前尺寸的桶只有 **1 个或 2 个** → **立即停止**，本次插入结束

最后一步：==**估计 1 的个数**：所有桶大小相加，**最老的桶只算一半**==（它可能有一半已滑出窗口，取中间值）。

## 4.4 手把手例子一（先练单步）

桶：$(4,60)(2,70)(1,75)(1,80)$，当前时间 81，$N = 30$，来了一个 1。
- 过期检查：$81-30 = 51$，最旧桶时间戳 60 > 51，不删。
- 建 $(1,81)$ → 尺寸 1 有三个：$(1,75)(1,80)(1,81)$ → 合并最旧两个 $(1,75)(1,80) \to (2,80)$（时间戳取 80！）→ 尺寸 2 现在有 $(2,70)(2,80)$ 两个，==停止。==
- 结果：$(4,60)(2,70)(2,80)(1,81)$。

## 4.5 手把手例子二（考试全尺寸，含过期和三级级联）

当前时间 100，$N=40$，桶 $(8,60)(4,72)(4,80)(2,85)(1,90)(1,95)$；101–110 流入 `0110100101`（1 在 102,103,105,108,110）。

- **t=101**（0）：过期检查 $101-40=61 \ge 60$ → **删 $(8,60)$**！（0 也要查过期，这是本题第一个坑。）
- **t=102**（1）：建 $(1,102)$ → 三个 1-桶 → 合并 $(1,90)(1,95) \to (2,95)$。
  桶：$(4,72)(4,80)(2,85)(2,95)(1,102)$
- **t=103**（1）：建 $(1,103)$。两个 1-桶，不动。
- **t=105**（1）：建 $(1,105)$ → 三个 1-桶 → 合并 $(1,102)(1,103) \to (2,103)$ → 三个 2-桶 → 合并 $(2,85)(2,95) \to (4,95)$ → 三个 4-桶 → 合并 $(4,72)(4,80) \to (8,80)$。**三级连环进位**。
  桶：$(8,80)(4,95)(2,103)(1,105)$
- **t=108**（1）：建 $(1,108)$。
- **t=110**（1）：建 $(1,110)$ → 三个 1-桶 → 合并 $(1,105)(1,108) \to (2,108)$。
  **最终：$(8,80)(4,95)(2,103)(2,108)(1,110)$**

自查两条不变式：尺寸从旧到新 8,4,2,2,1 不增 ✓；每种尺寸 ≤ 2 个 ✓。
估计 1 的个数：$\frac82 + 4 + 2 + 2 + 1 = 13$。

## 4.6 常见误区（复习课点名的三个）

- ✗ ==合并后==时间戳取旧的——必须==取**新**的==（时间戳定义是"桶内最新的 1"）；
- ✗ 忘了 ==0 比特也要做过期检查==；
- ✗ 窗口边界：t=208、N=60 时窗口是 **149–208**（60 个数），时间戳 148 恰好出界要删——数手指确认一次：208−60+1 = 149。

## 4.7 两个概念题的口袋答案

- 为什么空间 $O(\log^2 N)$：桶有 $O(\log N)$ 个（每种尺寸 ≤2、尺寸最大 $2^{\log N}$）；每桶存时间戳（$\bmod N$，$\log N$ 位）+ 尺寸指数（$\log\log N$ 位）≈ $O(\log N)$ 位。相乘。
- 为什么误差 ≤ 50%：不确定的只有最老桶（部分滑出窗口），它大小 $2^r$，估一半最多错 $2^{r-1}$；而窗口内至少还有后面各尺寸桶各一个，保底 $1+2+\cdots+2^{r-1} = 2^r - 1$ 个 1 → 相对误差不超一半。

## 🔴练习 4

接 4.5 最终状态，t=111 和 t=112 各来一个 1，桶变成什么？
**解**：t=111：无过期（111−40=71<80）；建 $(1,111)$ → 1-桶 = $(1,110)(1,111)$ 两个，不合并。t=112：建 $(1,112)$ → 三个 1-桶 → 合并 $(1,110)(1,111) \to (2,111)$ → 2-桶 = $(2,103)(2,108)(2,111)$ **三个** → 合并 $(2,103)(2,108) \to (4,108)$ → 4-桶 = $(4,95)(4,108)$ 两个，停。最终：$(8,80)(4,95)(4,108)(2,111)(1,112)$。

---

# 第 5 章 找高频元素——三个算法一次讲清

## 5.1 故事与免费送分结论

热搜系统：流量里找"==出现次数超过总量 $1/k$=="的元素（==heavy hitters==）。先领一个免费结论：**这样的元素至多 $k-1$ 个**。证明一句话：==若有 k 个各自超过 $n/k$，总次数就超过 n——矛盾==。（考试写这句就有分。）
再领一个辨析：heavy hitter ≠ top-k。=="前 10 名"永远存在；"超过 10% 的元素"可能一个都没有==。
坏消息（也是概念题）：想**一遍扫描 + 小内存 + 精确**解决，数学上不可能 → 所以三个算法都是近似的。

## 5.2 先热身：多数元素（k=2 的特例，Boyer-Moore 投票）

找"==出现超过一半=="的元素：==维护一个候选 + 一个计数==。新元素 = 候选 → 计数+1；≠ 候选 → 计数−1；计数归零 → 换候选。
**直觉**：不同元素两两抵消，真正过半的元素抵不完，最后必然站着。
**必须二次验证**：流 `B,B,Y,R,R` 最后候选是 R，但 R 只出现 2/5 不过半——==投票只保证"如果多数元素存在，它必是幸存者"，不保证幸存者是多数==。第二遍扫描数一下即可。（若题目**保证**多数元素存在，则一遍够——课上追问过。）

⚠️==注意：只能保证留下的candidate是多数元素，但不能保证是最多的。==

## 5.3 Misra-Gries：多数投票的推广（k−1 个槽）

**规则三分支**（元素 x 到达）：
1. x 已在槽 → 计数 +1；
2. x 不在槽、槽未满（< k−1 个）→ 新开槽，计数 1；
3. x 不在槽、槽满 → **全体槽计数 −1**，归零的清出。（x 自己**不进槽**——它的一次也被抵消了。）

**为什么不漏真高频**：每次触发第 3 条，相当于同时抹掉 k 个**不同**元素各一次（槽里 k−1 个 + 新来的 1 个）。这种"团灭"最多发生 $n/k$ 次，所以真实出现 > $n/k$ 次的元素不可能被抹到零。
**副作用**：==计数**只会偏低**==（被抵消掉的次数丢了）；槽里也可能混进假阳性（碰巧没被抵消完的低频元素）——==所以槽里的是**候选**，有条件就二次验证==。

**完整例子**（k=3 → 2 个槽，流 `1,1,2,3,4,5,1,1,1,5,3,3,1,1,2`，阈值 15/3 = 5）：

超过投票k/N次的元素？

⚠️解释=={1:2}==
- 1: 是进来的数字
- 2: 是该数字的counter
- 槽就是map里一共能放几个key

| 读入  | ==解释==⚠️                                                          | 动作     | 槽           |
| --- | ----------------------------------------------------------------- | ------ | ----------- |
| 1   | map为空，新开key<br>=> 此时计数为1，即 1:1                                    | 新开     | {1:1}       |
| 1   | 1已经存在，计数+1 <br>=> 1: 1+1 =>1:2                                    | +1     | {1:2}       |
| 2   | （1）2不存在 （2） 未满（key size < k-1 个）<br>=> 新开key，计数1<br>=> {1:2, 2:1} | 新开     | {1:2, 2:1}  |
| 3   | （1）3不在 （2）满<br>=> 全体槽计数-1<br>=> {1:1, 2:0} 归零需要清除key<br>=> {1:1}  | 满，全体−1 | {1:1}（2 清出） |
| 4   | (1) 4 不在 （2）key size <2<br>=> {1:1, 4:1}                          | 新开     | {1:1, 4:1}  |
| 5   | (1) 5不在 (2) key size =2<br>=> 全员-1<br>=> {1:0, 4:0}<br>=> 全员清除 {} | 满，全体−1 | { }（全清）     |
| 1   | {1:1}                                                             | 新开     | {1:1}       |
| 1,1 | {1:2} -> {1:3}                                                    | +1,+1  | {1:3}       |
| 5   | {1:3, 5:1}                                                        | 新开     | {1:3, 5:1}  |
| 3   | {1:2, 5:0} -> {1:2}                                               | 满，全体−1 | {1:2}       |
| 3   | {1:2, 3:1}                                                        | 新开     | {1:2, 3:1}  |
| 1,1 | {1:4, 3:1}                                                        | +1,+1  | {1:4, 3:1}  |
| 2   | {1:3}                                                             | 满，全体−1 | {1:3}       |

最终候选 {1:3}。元素 1 真实出现 7 次 > 5 ✓（计数 3 < 7，低估）。
⚠️ ⚠️  几个注意点
- 5 = N/k = 15/3
- 上面的表可以得出结论：1是heavy hitter（但不能保证是最多的），这个算法只能保证：
(真实次数−k/n)​≤槽里的计数≤真实次数
- 另外这个算法无法统计他出现了几次，需要扫第二轮来统计出现的次数，也就是second pass。

## 5.4 Lossy Counting：定时漏水

MG 是"槽满才扣"，LC 改成"**定时扣**"：==把流切成窗口，每窗 $\frac1\varepsilon$ 个元素==；窗口内正常计数（新元素直接进表）==；**每个窗口结束，全表 −1、归零删除**。==低频元素活不过几个窗口（"lossy" 有损之名由此来），高频元素扣不完。
**误差保证**：每个计数最多被扣"窗口个数"次 = $\varepsilon n$ 次 → 低估、且误差 ≤ $\varepsilon n$。$\varepsilon$ 越小越准但表越大——精度换内存。

**完整例子**同上（k=3 → 2 个槽，流 `1,1,2,3,4,5,1,1,1,5,3,3,1,1,2`，$\varepsilon = 0.2$ → 每窗 $1/0.2 = 5$ 个，共 3 窗）：==直接按顺序切==

- 窗 1 `[1,1,2,3,4]`：表 {1:2, 2:1, 3:1, 4:1} → 全体−1 → **{1:==1==}**
- 窗 2 `[5,1,1,1,5]`：==接着累加== {1:==4==, 5:2} → −1 → **{1:3, 5:1}**
	- 此处的4=上一步的1+当前window的3
- 窗 3 `[3,3,1,1,2]`：累加 {1:5, 3:2, 5:1, 2:1} → −1 → **{1:4, 3:1}**

答案：高频候选 1（估计 4）。验证：真实 7，$7-4 = 3 \le \varepsilon n = 0.2 \times 15 = 3$ ✓。
**答题格式**：像上面一样每窗写两行——"累加后的表"和"减 1 后的表"，一行都别省。

==f ≥ (s−ε)N==
- s: support threshold
- f: last result
- ε: error parameter
- f: estimated count, 就是统计出来的冒号后面的数字，
- 只有 f >= (s-ε) * N 时候，才说明under-estimate

## 5.5 Count-Min Sketch：从不扣分，改成"多问几个人取最小"

前两个靠"扣分"腾地方，CMS 换思路：**固定大小的 $d \times w$ 计数格子阵**，第 i 行配哈希 $h_i$。
- **Inc(x)**：每行 i，$CMS[i][h_i(x)]$ +1（d 行各加一次）。
- **Count(x)**：读出这 d 个格子，**取最小**：$\hat f(x) = \min_i CMS[i][h_i(x)]$。
[[CountMinSketch_例题详解✅]]

⚠️
- 现在 cat 来了。规则是"**每一行都要放一个珠子**"，问题只是每行放在哪一列——**这就去问管那一行的哈希函数**：
	- 行 1 归 h1h_1 h1​ 管 → 算 $h1(cat)=21  5=1h_1(\text{cat}) = 21 \bmod 5 = 1 h1​(cat)=21mod5=1$ → 放**行 1 的 col1**
	- 行 2 归 h2h_2 h2​ 管 → 算 $h2(cat)=3  5=3h_2(\text{cat}) = 3 \bmod 5 = 3 h2​(cat)=3mod5=3$ → 放**行 2 的 col3**
	- 行 3 归 h3h_3 h3​ 管 → 算 $h3(cat)=2  5=2h_3(\text{cat}) = 2 \bmod 5 = 2 h3​(cat)=2mod5=2$ → 放**行 3 的 col2**
```
				  col0   col1   col2   col3   col4
 行1 ← h₁         [  ]   [+1]   [  ]   [  ]   [  ]     ← h₁(cat)=1
 行2 ← h₂         [  ]   [  ]   [  ]   [+1]   [  ]     ← h₂(cat)=3
 行3 ← h₃         [  ]   [  ]   [+1]   [  ]   [  ]     ← h₃(cat)=2
```


![[Pasted image 20260812130957.png]]




**为什么取 min（必考解释）**：每个格子 = x 的真实次数 + 撞进来的别人的次数。所以每个格子都**只多不少**。d 个"只多不少"的读数里，最小的那个 = 被污染最轻的 → 最接近真相，且仍然 ≥ 真值。结论：**CMS 只会高估**。
参数直觉：$w$（列数）越大越少撞 → 控制误差；$d$（行数）越多"全部 d 行都被大流量污染"越难 → 控制失败概率。


## 5.6 三兄弟终极对比表（辨析题的满分答案）

|      | Misra-Gries  | Lossy Counting | Count-Min Sketch                     |
| ---- | ------------ | -------------- | ------------------------------------ |
| 内存   | k−1 个槽       | 计数表（受 ε 控制）    | 固定 d×w 矩阵<br>（d是hash函数个数决定的，w是题目指定的） |
| 扣分时机 | 槽满且新人来（事件触发） | 每窗末尾（定时触发）     | 从不                                   |
| 估计方向 | **低估**       | **低估**         | ==**高估**（取 min）==                    |
| 误差保证 | —            | ≤ εn           | 由 w 控制                               |
| 假阳性  | 有（候选需验证）     | 有              | 查询谁都给个数                              |

## 🔴练习 5

流 `[a,b,a,c,a,b,a,a]`：(a) 用 MG（k=3，2 槽）跑完；(b) 用 LC（ε=0.25，窗=4）跑完。
**解 (a)**：a:1 → a:1,b:1 → a:2,b:1 → c 来满员全体−1 → a:1 → a:2 → a:2,b:1 → a:3,b:1 → a:4,b:1。最终 {a:4, b:1}（a 真实 5 次）。
**解 (b)**：窗1 `[a,b,a,c]`：{a:2,b:1,c:1} → −1 → {a:1}。窗2 `[a,b,a,a]`：{a:4,b:1} → −1 → {a:3}。最终 {a:3}，误差 5−3=2 ≤ εn=2 ✓。

---

# COMP9313 算法零基础详解（下册：相似项三部曲 + 图两件套 + 一周计划）

> 接上册（第 0–5 章）。下册：第 6–8 章相似项流水线（Q5）、第 9–10 章图算法（Q6）、第 11 章一周消化计划 + 综合自测卷。
> 提醒：第 6–8 章是一个连续的故事，一口气读完效果最好（约 2.5 小时）。

---

# 第 6 章 相似项第一步：把文档变成集合（Shingling + Jaccard）

## 6.1 大故事（三章的总剧本，先看懂再出发）

任务：一百万篇网页找出所有=="近似重复"的对==（抄袭检测、搜索去重）。三个拦路虎：

1. 文档是**文本**，"相似"怎么量化？→ 第 6 章：==变成**集合**，用 Jaccard 度量==。
2. 每个集合可能有几万个元素，一百万个集合存不下、比不动 → 第 7 章：压成**短签名**，还得保住相似度信息。
3. 一百万篇两两配对 = $\binom{10^6}{2} \approx 5 \times 10^{11}$ 对，每微秒比一对也要 6 天 → 第 8 章：**只比"有希望"的对**。

记忆锚点：**表示 → 压缩 → 少比**。

## 6.2 k-shingle：滑窗切片

拿==长度 k 的窗口==从文档头滑到尾，==每个窗口的内容是一个 **k-shingle**==（也叫 k-gram）。==token 可以是字符或单词==（考试例子都是字符）。结果取**集合**——**重复只算一次**。

k=窗口的长度，例：k=2，文档 "abcab"：窗口依次 ab、bc、ca、ab → 集合 $S = \{ab, bc, ca\}$，**3 个**（第二个 ab 不重复计）。这个去重是 Q5 第一小问的核心考点。
⚠️
==长度 L 的文档有 $L - k + 1$ 个窗口==（"abcab" 5 个字符 → 4 个窗口）。

**为什么这样设计**：
- 保住了**语序**："Spain beat Argentina" vs "Argentina beat Spain"—==—按单词集合看一模一样（相似度 1），按 2-shingle 看完全不同（相似度 0）==；
- **局部改动只有局部影响**：改一个词，只有覆盖它的 k 个窗口变化，其余 shingle 原封不动——==所以小改动的文档 shingle 集合大量重叠==。
- k 的选择：太小（k=1,2）人人都共享大量 shingle，区分不开；太大则轻微改动破坏太多。经验：短文档 k≈5，长文档 k≈10（概念题一句话）。

## 6.3 Jaccard 相似度：交并比

$$\text{sim}(A, B) = \frac{|A \cap B|}{|A \cup B|}$$

"共同拥有的 ÷ 总共出现过的"。取值 0（毫无交集）到 1（完全相同）。距离 = 1 − 相似度。本课**只考这一个**度量（欧氏、余弦、编辑距离明确不考）。

例：$A = \{ab, bc, ca\}$，$B = \{ab, bc, cd\}$：交 = {ab, bc} 2 个，并 = {ab, bc, ca, cd} 4 个 → sim = 2/4 = 0.5。

## 6.4 考试题型：最小 k 值
![[Pasted image 20260816174404.png|700]]
the total number of distinct k-shingles is 9 的最小k值是什么？
题目也等于：
1）`k = shingle 长度`、`9 = 三个文档去重后的 shingle 总数（并集）`
2）给 3 个文档，求使 distinct shingle 总数为 X 的最小 k，并列出全部 shingle。

**解法固定**：从 k=1 起逐个试；每个 k 下逐文档列 shingle，**文档内去重 + 跨文档去重**（跨文档重复的画删除线，学 sample solution 的格式），数**并集**大小；命中 X 就停，并顺手说明 k−1 时为什么不行。

**手把手**（sample 原题）：D1 = "abcbacb"、D2 = "cbaaabc"、D3 = "acbaaac"，目标 9
- k=1：并集 {a,b,c} = 3，不够
- k=2：D1 → ab,bc,cb,ba,ac；D2 → cb̶,b̶a̶,aa,ab̶（新增 aa）；D3 → ac̶,cb̶,ba̶,aa̶（无新增）。并集 {ab,bc,cb,ba,ac,aa} = 6，不够。
- k=3：D1 → abc,bcb,cba,bac,acb（5 个）；D2 → c̶b̶a̶,baa,aaa,aab,a̶b̶c̶（新增 3）；D3 → a̶c̶b̶,c̶b̶a̶,b̶a̶a̶,a̶a̶a̶,aac（新增 1）。合计 5+3+1 = **9** ✓ → 答案 k=3。

## 🟢练习 6

D1="banana"、D2="bandana"，k=3：各自的 shingle 集合、Jaccard 相似度？
**解**：D1（6 字符 → 4 窗）：ban, ana, nan, ana → {ban, ana, nan}（3 个，ana 去重）。D2（7 字符 → 5 窗）：ban, and, nda, dan, ana → {ban, and, nda, dan, ana}（5 个）。交 = {ban, ana} 2 个；并 = {ban, ana, nan, and, nda, dan} 6 个 → sim = 2/6 = **1/3**。

---

# 第 7 章 相似项第二步：MinHash——把大集合压成短指纹

## 7.1 先把数据摆成一张 0/1 大表

把所有文档的 shingle 集合排成**特征矩阵**：==行 = 宇宙中出现过的每个 shingle==，==列 = 每个文档==；==格子 = 1 表示"该文档含该 shingle"==。一列就是一个文档的=="0/1 指纹"==，可能有百万行——太长了，要压缩。

## 7.2 神奇的性质（本章唯一必背的东西）

把==**行随机洗牌**==，然后看==每列=="==从上往下第一个 1 出现在第几行=="——==这个数叫该列的 **MinHash 值==**。奇迹是：

$$P\big[\,\text{minhash}(C_1) = \text{minhash}(C_2)\,\big] = \text{Jaccard}(C_1, C_2)$$

**为什么（听懂即可，证明不考）**：从上往下找，第一个碰到"两列并集中的 1"的那一行，决定了两列的 minhash 是否相等——相等当且仅当这一行**两列都是 1**（即落在交集里）。洗牌是随机的，所以"第一个并集行落在交集里"的概率 = $\frac{|交|}{|并|}$ = Jaccard。就这么直接。

于是：==洗 K 次牌得到 K 个数 = 长度 K 的**签名向量==**。==两列签名"逐位相同的比例" ≈ Jaccard 相似度（K 次独立试验的平均）==。百万行 → 100 个数，相似度信息还在。**K 越大估计越准**——考试小例子里 K=2，估出来的相似度和真实值差很远是正常的，这本身就是个考点观察。

## 7.3 实际怎么算：不洗牌，用哈希函数假装洗牌

真洗百万行的牌太贵。改用哈希函数 $h_i(r) = (a \cdot r + b) \bmod N$ 给每行发一个"洗牌后的新行号"。算法只有一句口诀：

> **逐行扫描，遇 1 更新，取最小。**

1. 每列 × 每个哈希的签名槽全部初始化 $\infty$；
2. 扫第 r 行：先算出所有 $h_i(r)$；
3. 该行值为 1 的每一列 C、每个哈希 i：$\text{sig}(C)[i] \leftarrow \min(\text{sig}(C)[i],\ h_i(r))$；
4. 扫完结束。（若所有槽都已是 0，可提前停——不可能更小了。讲师夸过这个观察。）

## 7.4 手把手完整例子（sample exam 原题）

7 行；$h_1(n) = (3n + 2) \bmod 7$；$h_2(n) = (2n - 1) \bmod 7$。
$C_1 = [0,1,0,0,1,1,1]$（行 1,4,5,6 为 1）；$C_2 = [1,0,1,0,1,1,0]$（行 0,2,4,5 为 1）。

**第一步永远是列哈希值表**（考场上先干这个，之后照抄不用心算）：

| n                      | ==0==           | 1   | 2   | 3   | 4   | 5   | 6   |
| ---------------------- | --------------- | --- | --- | --- | --- | --- | --- |
| $h_1 = (3n+2) \bmod 7$ | 2               | 5   | 1   | 4   | 0   | 3   | 6   |
| $h_2 = (2n-1) \bmod 7$ | -1 -> ==**6**== | 1   | 3   | 5   | 0   | 2   | 4   |

注意 $h_2(0) = -1 \bmod 7 = 6$——==负数取模加回 7==（上册 0.2 节的坑）。

**逐行扫**（只写有动作的行）：

| 行      | 谁是 1 | 哈希值 $(h_1, h_2)$ | Sig1               | Sig2                              |
| ------ | ---- | ---------------- | ------------------ | --------------------------------- |
| ==初始== | —    | —                | $[\infty, \infty]$ | $[\infty, \infty]$                |
| ==0==  | 只 C2 | (2, 6)           | 不变                 | $[2, 6]$                          |
| 1      | 只 C1 | (5, 1)           | $[5, 1]$           | 不变                                |
| 2      | 只 C2 | (1, 3)           | 不变                 | $[\min(2,1), \min(6,3)] = [1, 3]$ |
| 3      | 都是 0 | —                | 跳过                 | 跳过                                |
| 4      | 都是 1 | (0, 0)           | $[0, 0]$           | $[0, 0]$                          |
| 5, 6   | —    | —                | 不可能比 0 小，可停        | 同左                                |

**最终：$\text{Sig}_1 = [0,0]$，$\text{Sig}_2 = [0,0]$。**

**附加观察（答上有加分）**：⚠️⚠️⚠️⚠️
==两签名完全相同== → 估计相似度 1；
回到人工分析：
C1 的 1 在行 {1,4,5,6}，
C2 的 1 在行 {0,2,4,5}，
交集 {4,5} = 2 个，
并集 {0,1,2,4,5,6} = 6 个 
→ **真实相似度 =2/6=1/3**
差得远，因为只有 2 个哈希——样本太少方差大，这正是实际用 K=100 的原因。


### 考试怎么写

如果题目追问"你的估计值和真实 Jaccard 为什么不一样"，写这两句就够了：

> The signature estimate is unbiased but has high variance when the number of hash functions is small. With only 2 hash functions the estimate can only take the values 0, 0.5 or 1, so it cannot approximate the true Jaccard of 1/3. Increasing K reduces the variance.

## 7.5 常见误区

- ✗ 哈希函数的输入是==**行号** n==，不是格子的值；
- ✗ 该行为 0 的列也去更新——只有**为 1** 的列才更新；
- ✗ ==负数取模==；✗ 忘了"取 min"写成"覆盖"。

## 🟢练习 7

5 行；$h_1(n) = n + 1 \bmod 5$、$h_2(n) = 3n + 1 \bmod 5$；$C_1=[1,0,0,1,0]$、$C_2=[0,0,1,0,0]$。求签名。
**解**：值表 $h_1$: 1,2,3,4,0；$h_2$: 1,4,2,0,3。行 0（C1）：Sig1=[1,1]。行 2（C2）：Sig2=[3,2]。行 3（C1）：$h=(4,0)$ → Sig1=[min(1,4), min(1,0)]=[1,0]。**Sig1=[1,0]，Sig2=[3,2]**。（这就是课件 9.52–9.53 的 S1/S2 列。）

---

# 第 8 章 相似项第三步：LSH——只比"有希望"的对

## 8.1 聪明的点子：切段撞桶

签名再短，一百万列两两比较还是 $5\times10^{11}$ 对。LSH 的赌法：

> 把每列的签名**竖着切成 b 段（band），每段 r 行**（$b \times r = K$）。对每段做哈希扔进桶。**只要有任何一段，两列完全相同（撞进同一桶），这两列就成为候选对**，留待精确验证；从没撞过的对，直接不看。

**为什么赌得赢**：两列越相似 → 签名逐位相同比例 $s$ 越高 → 某段 **r 位连续全同**的概率 $s^r$ 越大 → b 段里至少中一段的机会越大。不相似的列很难凑齐连续 r 位全同——它们几乎从不成为候选。相似的"总有一段像"，不相似的"段段都不像"。

## 8.2 公式与四步推导（考试要写过程）

设两列真实相似度 $s$：

1. 一行相同的概率 = $s$（第 7 章的神奇性质）；
2. 一段（r 行）全同 = $s^r$（独立相乘，上册 0.3）；
3. 该段不全同 = $1 - s^r$；所有 b 段都不全同 = $(1-s^r)^b$；
4. **至少一段全同（成为候选）= $1 - (1 - s^r)^b$**（"至少→取反"，上册 0.3）。

防呆口诀：**先段内（r 在内层指数），后段间（b 在外层指数）**。

## 8.3 手把手例子

**例 1（sample 原题）**：K=10，b=5 段 × r=2 行，s=0.6。
$$P = 1 - (1 - 0.6^2)^5 = 1 - 0.64^5 \approx 0.893$$
写出表达式即满分；心里有数 ≈ 89%。

**例 2（课上算过的一对，理解 FP/FN）**：b=20, r=5。
- 真相似对 s=0.8：$P(\text{漏掉}) = (1-0.8^5)^{20} \approx 0.00035$ → 假阴性约 **0.035%**（几乎不漏）；
- 不相似对 s=0.3：$P(\text{误抓}) = 1-(1-0.3^5)^{20} \approx 0.0474$ → 假阳性约 **4.7%**（多做点验证而已，可接受）。

## 8.4 概念配套（简答常问）

- 同桶 = **候选**（"probably identical"，仍需精确算一次 Jaccard 验证——==LSH 输出的只是 candidates==）；不同桶 = 在**该段**上肯定不同（别的段还有机会）。
- 调参方向：==**r 大 → 门槛严**==（候选少、漏网多）；==**b 大 → 门槛松**==（不易漏、候选多）。S 形曲线的跳变阈值 ≈ $(1/b)^{1/r}$。
- 两类错误谁更糟：假阴性（相似的漏了）是真损失；假阳性只是浪费验证时间。

## 练习 8

K=12，4 段 × 3 行，s=0.5：候选概率表达式？粗算数值？
**解**：$1 - (1 - 0.5^3)^4 = 1 - 0.875^4 \approx 0.414$。（顺带体会：s=0.5 在这组参数下只有四成机会被抓——阈值 $(1/4)^{1/3} \approx 0.63$ 比 0.5 高。）

---

# 第 9 章 图算法一：MapReduce 上的最短路径（并行 BFS）

## 9.1 故事

社交网络几十亿节点，求"从 s 到所有人的最短距离"。单机标准答案是 Dijkstra：维护一个**全局优先队列**，每次取出当前最近的节点向外扩展。但 MapReduce 的 mapper/reducer 分布在不同机器、**不共享内存**——全局队列造不出来。（考试概念题原话答案："we cannot maintain a global queue"。）

## 9.2 聪明的点子：一轮扩散一跳

放弃"聪明地挑下一个"，改成"**所有人同时向邻居喊话**"：每轮 MapReduce，每个节点把"==我离 s 多远=="广播给邻居；==邻居取所有来话的最小值更新自己==。一轮 = 距离信息向外传一跳。==像水波纹扩散——慢（重复劳动多）==，但==每一步都可完全并行==。这就是==并行 BFS==。

## 9.3 数据怎么长

每行一个节点：`节点 --> 当前距离 | 邻接表(邻居:边权)`。初始：源点 0，其他 ∞。

## 9.4 每轮怎么跑（**必背骨架**）

**Mapper** 读一个节点 n（距离 d），发**两类**消息：
1. `(n, 邻接表)` —— **把图结构原样传下去**。为什么必须发：本轮 reducer 的输出就是下轮的输入，不带上结构，下一轮 mapper 就没有邻接表可读了。**忘发结构 = 历届最经典扣分点**；
2. 对每个邻居 m：`(m, d + w(n,m))` —— "经过我，你能以这个距离到达"。

**Reducer** 收节点 m 的一堆消息：用 IsNode 区分两类——是结构就恢复邻接表；是数字就参与**取 min**（含 m 的旧距离）。输出完整节点 `(m, 新距离, 邻接表)`。

**终止**：某轮**没有任何节点距离变小** → 收敛。检测手段：reducer 每次更新距离就把 **Counter** +1；driver（外层循环程序）每轮结束读 counter，为 0 就停。
（⚠️ 不能用"没有 ∞ 了就停"——带权图中边数少 ≠ 距离短，后面的轮次还可能刷出更短的路。）

## 9.5 手把手例子（往届期末原题同款，把格式练成肌肉记忆）

```
s --> 0 | a:4, b:1
a --> ∞ | c:2
b --> ∞ | a:2, c:6
c --> ∞ |
```
⚠️看这个： [[BFS 最短路径✅]]

**两个必须体会的现象**：①中间轮次的值不是最终值（a 从 4 改到 3）；②改善会**传染**（a 变小导致下一轮 c 也变小）——所以必须跑到"整轮无更新"。

## 9.6 概念题弹药库

- 迭代次数：无权图 = **图的直径**（"六度分隔"成立 → 全球社交网络 ≤ 6 轮）；带权图最坏 **|V|−1** 轮（课件 9 节点链 + 权 10 近道的例子要 8 轮）；
- "节点第一次被发现即最短"——无权图对，**带权图错**（判断题）；
- 权重必须为正：负权 + 环 → 距离无限变小不终止；
- 要路径本身：mapper 把路径连同距离一起发（课件推荐），或存前驱回溯（低效）；
- 优化：节点带 status 标记，上轮没变的不再发距离消息（frontier 思想）；
- Dijkstra vs 并行 BFS：前者高效无浪费、后者并行但大量重复——分布式的代价。

## 练习 9

```
s --> 0 | x:2, y:6
x --> ∞ | y:3
y --> ∞ |
```
写出每轮直到终止。
**解**：轮1：x=2, y=6。轮2：x(2) 发 (y, 5) → y = min(6,5) = **5**。轮3：无更新，终止。y 的最短路是 s→x→y=5，不是直连的 6——两轮就演示了"绕路更短"。

---

# 第 10 章 图算法二：PageRank——链接就是投票

## 10.1 故事

搜索引擎要给网页排重要性。==数"被链接次数"==？太好刷——自己建 1000 个小号页面互链就上位。Google 的两个修正：
1. **票有轻重**：被重要页面链接，才算重票（递归定义——重要性来自重要页面的背书）；
2. **票会稀释**：一个页面把自己的分数**平均分给**所有出链。出度 $d_i$ 的页面，每条出链携带 $\frac{r_i}{d_i}$。

于是：$r_j = \sum_{i \to j} \frac{r_i}{d_i}$（所有链向 j 的页面各交一份）。

## ⭕️ 详细计算过程 [[page rank 推导✅]]
## 10.2 怎么解：幂迭代（power iteration）

方程组没常数项 → 没有唯一解（(2,2,1) 和 (4,4,2) 都满足）→ **加约束 $\sum r_i = 1$**。
解法不用解方程，用**反复迭代**：人人先拿 $\frac1N$，然后所有页面同时"发钱-收钱"一轮，反复直到数值不再变（变化量 < 阈值，或跑固定轮数）。

**手算一轮**（y/a/m 例子：y→y, y→a；a→y, a→m；m→a）：
初始 (1/3, 1/3, 1/3)。$d_y = 2, d_a = 2, d_m = 1$。
- $r_y^{new} = \frac{r_y}{2} + \frac{r_a}{2} = \frac16 + \frac16 = \frac13$
- $r_a^{new} = \frac{r_y}{2} + r_m = \frac16 + \frac13 = \frac12$
- $r_m^{new} = \frac{r_a}{2} = \frac16$
多轮后收敛到 $(\frac25, \frac25, \frac15)$。**考试会算一到两轮就够。**


## 10.3 两个坑（必考辨析）

- **==Dead end==（死胡同）**：==页面没有出链==。分数进去就**蒸发**——每轮总分变少，最后全体归零。**这是真问题**（数学上矩阵不再"列和为 1"，理论保证失效）。
- **==Spider trap==（蜘蛛陷阱）**：一小圈页面只==互相链==。分数进去**出不来**，最终吸干全网。数学上"不是问题"（依然收敛）——只是收敛到的结果没意义。

**一句话区分**：==dead end 漏分==（总量崩），==spider trap 吞分==（分布崩）。

## 10.4 统一解药：teleport（随机跳转）

上网的人不会永远点链接——有时直接输网址跳走。建模：==每步以概率 $\beta$（0.8~0.9）沿链接走，以概率 $1-\beta$ **随机跳到任意页面**==。陷阱困不住会跳的人；==死胡同的人被强制跳走==。最终公式（**必背**）：

$$r_j = \beta\left(\sum_{i \to j} \frac{r_i}{d_i} + \frac{r_{\text{dead end}}}{N}\right) + \frac{1-\beta}{N}$$

**课程规定的操作顺序**（复习课反复强调）：**先处理 dead end**——把它的分数按 $\frac1N$ 分给全图**包括它自己**（矩阵语言：把那一整列换成全 $\frac1N$）；**再套 teleport**。

## 10.5 考试主打题型：列方程（一步步来）

**步骤**：
① 列每个节点的出度，圈出 dead end；
② 每个节点一条方程，套模板；
③ 三查：dead end 项**每条方程都有**（含它自己那条）、跳转项分母是 **N** 不是出度、系数 $\frac{1}{d_i}$ 用**源节点**（给钱的人）的出度。

**手把手**（5 节点：1→2,3；2→3,4,5；3→1,4；4→1,5；5 无出边；β=0.8）：
出度：$d_1=2, d_2=3, d_3=2, d_4=2$；dead end = 5；跳转项 $\frac{0.2}{5}$。

$$r(1) = 0.8\left(\tfrac12 r(3) + \tfrac12 r(4) + \tfrac15 r(5)\right) + \tfrac{0.2}{5}$$
$$r(2) = 0.8\left(\tfrac12 r(1) + \tfrac15 r(5)\right) + \tfrac{0.2}{5}$$
$$r(3) = 0.8\left(\tfrac12 r(1) + \tfrac13 r(2) + \tfrac15 r(5)\right) + \tfrac{0.2}{5}$$
$$r(4) = 0.8\left(\tfrac13 r(2) + \tfrac12 r(3) + \tfrac15 r(5)\right) + \tfrac{0.2}{5}$$
$$r(5) = 0.8\left(\tfrac13 r(2) + \tfrac12 r(4) + \tfrac15 r(5)\right) + \tfrac{0.2}{5}$$

自查演示：源节点 2 的系数出现在 r(3)、r(4)、r(5)，各 $\frac13$，加起来 = 1 ✓（一个页面发出去的钱总和是它的全部分数——快速验算法）。


⚠️这句话意思是：2 有3个来源234，此时我们看r3,r4,r5 里的r2 系数，刚好都是1/3，加起来等于1。 这是个快速验证的方法
![[Pasted image 20260816095848.png]]

## 10.6 MapReduce 版（会写伪代码 + 跑一轮数值）

和并行 BFS **同一个骨架**，只换两处：mapper 发的数值消息从"距离 d+w"换成"**分数 $\frac{r_n}{d_n}$**"；reducer 的聚合从"取 min"换成"**求和**"。完整版两处修正：**mapper 治 dead end**（空邻接表节点向全图每个节点发 $\frac{r_i}{N}$）、**reducer 治 teleport**（最后算 $\beta \cdot s + \frac{1-\beta}{N}$）。

**跑一轮数值**（课件例子，5 节点各 0.2 起步；`n1→[n2,n4]；n2→[n3,n5]；n3→[n4]；n4→[n5]；n5→[n1,n2,n3]`，简化版无 teleport）：
发钱：n1 → n2、n4 各 0.1；n2 → n3、n5 各 0.1；n3 → n4 0.2；n4 → n5 0.2；n5 → n1、n2、n3 各 0.0667。
收钱：n1 = 0.0667；n2 = 0.1+0.0667 = 0.167；n3 = 0.1+0.0667 = 0.167；n4 = 0.1+0.2 = 0.3；n5 = 0.1+0.2 = 0.3。
（自查：总和 = 1 ✓，无 dead end 时分数守恒。）

## 🟢练习 10

3 节点：1→2；2→1, 2→3；3 无出边。β=0.8。列三条方程。
**解**：$d_1 = 1, d_2 = 2$；dead end = 3；跳转项 $\frac{0.2}{3}$。
$r(1) = 0.8(\tfrac12 r(2) + \tfrac13 r(3)) + \tfrac{0.2}{3}$
$r(2) = 0.8(r(1) + \tfrac13 r(3)) + \tfrac{0.2}{3}$
$r(3) = 0.8(\tfrac12 r(2) + \tfrac13 r(3)) + \tfrac{0.2}{3}$
（检查：$\frac13 r(3)$ 三条都有 ✓；源 2 的系数 $\frac12 + \frac12 = 1$ ✓。）





